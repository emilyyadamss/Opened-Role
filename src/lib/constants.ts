/**
 * Shared password for the seeded demo accounts. Real sign-ups choose their own
 * password; this only exists so the demo accounts (emily@openedrole.dev, etc.)
 * are easy to sign into. See the hint on the login page.
 */
export const DEMO_PASSWORD = 'openedrole'
